const CONFIG = {
  BASE_URL: 'https://story-api.dicoding.dev/v1',
  API_TOKEN: '', // Optional: will use token from localStorage if set
  // Isi dengan VAPID public key dari dokumentasi API (Base64 URL-safe)
  VAPID_PUBLIC_KEY: '',
};

export default CONFIG;
